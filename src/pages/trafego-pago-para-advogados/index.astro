---
import BaseLayout from '@layouts/BaseLayout.astro';
import Button from '@components/ui/Button.astro';
import LegalLogosMarquee from '@components/sections/LegalLogosMarquee.astro';
import {
  legalAreas,
  legalOffer,
  legalTeam,
  oabCompliance,
} from '@data/legalSeo';
import { brand, getCanonicalUrl, organizationId, websiteId } from '@data/seo';
import {
  ArrowRight,
  BadgeCheck,
  BarChart3,
  Eye,
  FileText,
  Gauge,
  Landmark,
  Layers,
  LineChart,
  MousePointerClick,
  Search,
  ShieldCheck,
} from 'lucide-react';

const canonical = getCanonicalUrl('/trafego-pago-para-advogados');
const pageDescription =
  'Tráfego pago para advogados com campanhas de fundo de funil, landing pages ultrarrápidas, qualificação inteligente e comunicação alinhada à OAB.';

const heroMetrics = [
  {
    value: 'Google Ads',
    label: 'fundo de funil',
    Icon: Search,
  },
  {
    value: 'OAB',
    label: 'rigor ético',
    Icon: ShieldCheck,
  },
];

const conversionCards = [
  {
    title: 'Verba',
    desc: 'Agência generalista compra volume. No jurídico, volume barato costuma virar triagem cara, WhatsApp lotado e equipe comercial cansada.',
    Icon: MousePointerClick,
  },
  {
    title: 'Filtro',
    desc: 'A campanha já nasce com intenção, negativas e linguagem de qualificação para reduzir curiosos antes do primeiro contato.',
    Icon: Gauge,
  },
  {
    title: 'WhatsApp',
    desc: 'O contato chega com mais contexto, área, urgência e fit comercial. Menos atendimento desperdiçado, mais conversa com chance real.',
    Icon: FileText,
  },
  {
    title: 'Contrato',
    desc: 'A leitura não termina no lead. Acompanhamos origem, qualidade, atendimento e sinais que aproximam mídia de honorários.',
    Icon: LineChart,
  },
];

const technologyDifferentials = [
  {
    title: 'Milissegundos valem contratos',
    desc:
      'Desenvolvemos landing pages em Astro e JAMstack para que o clique pago encontre uma página leve, estável e pronta para converter no celular.',
    Icon: Layers,
  },
  {
    title: 'Grafite, silêncio visual e respeito',
    desc:
      'A estética é parte da venda: grafite, contraste limpo e minimalismo institucional criam autoridade sem parecer anúncio desesperado.',
    Icon: Eye,
  },
  {
    title: 'O fim do achismo comercial',
    desc:
      'Mapeamos clique no WhatsApp, formulário, origem da campanha e sinais de qualidade para saber qual palavra-chave trouxe oportunidade real.',
    Icon: BarChart3,
  },
];

const specialtyHighlights = [
  {
    title: 'Advocacia Criminal: urgência e sigilo',
    href: '/trafego-pago-para-advogados/advogados-criminalistas',
    desc:
      'O mercado criminal não perdoa lentidão. Capturamos o momento da dor sem sensacionalismo, com página rápida, mensagem discreta e foco em conversa válida.',
    metrics: ['custo por conversa válida', 'tempo até primeiro atendimento'],
  },
  {
    title: 'Direito Previdenciário: triagem documental',
    href: '/trafego-pago-para-advogados/advogados-previdenciarios',
    desc:
      'Volume vazio quebra a operação previdenciária. O funil separa curiosidade de demanda madura e favorece contatos com documentos e contexto para análise.',
    metrics: ['custo por lead com documento', 'CNIS e laudos prontos'],
  },
  {
    title: 'Tributário e Empresarial: aquisição B2B',
    href: '/trafego-pago-para-advogados/advogados-empresariais',
    secondaryHref: '/trafego-pago-para-advogados/advogados-tributaristas',
    desc:
      'CEO e diretor financeiro não respondem a anúncio amador. O funil filtra porte, cargo, escopo e problema antes da conversa comercial.',
    metrics: ['custo por decisor', 'porte da empresa'],
  },
  {
    title: 'Família e Imobiliário: clareza patrimonial',
    href: '/trafego-pago-para-advogados/advogados-de-familia',
    secondaryHref: '/trafego-pago-para-advogados/advogados-imobiliarios',
    desc:
      'Demandas sensíveis exigem discrição e definição. Segmentamos divórcio, guarda, inventário, locação e bloqueios patrimoniais sem misturar dores diferentes.',
    metrics: ['custo por demanda definida', 'privacidade no contato'],
  },
];

const faqs = [
  {
    question: 'Por que a Scale não trata lead jurídico como lead de e-commerce?',
    answer:
      'Porque contratação jurídica depende de intenção, urgência, confiança, capacidade de pagamento e compliance. Por isso a operação une mídia, página, triagem e leitura comercial, em vez de otimizar só para lead barato.',
  },
  {
    question: 'Quanto tempo até uma campanha começar a gerar demanda?',
    answer:
      'Campanhas de fundo de funil em Google Ads podem gerar contatos nas primeiras semanas, mas resultado financeiro depende da área, do atendimento e do ticket. Criminal tende a ter decisão mais rápida; consultivo empresarial costuma exigir maturação.',
  },
  {
    question: 'Por que construir a landing em Astro melhora o ROI?',
    answer:
      'Porque clique pago não tolera página lenta. Uma landing leve, estável e objetiva reduz abandono, melhora a experiência no celular e dá mais chance para cada real de mídia virar conversa qualificada.',
  },
  {
    question: 'A estratégia respeita o Provimento 205/2021?',
    answer:
      'Sim. A comunicação evita promessa de resultado, mercantilização, sensacionalismo e comparação depreciativa. Criativos, páginas e textos são validados com o escritório antes da veiculação.',
  },
];

const schema = {
  '@context': 'https://schema.org',
  '@type': 'Service',
  '@id': `${canonical}#service`,
  name: 'Tráfego Pago para Advogados',
  alternateName: legalOffer.name,
  description: pageDescription,
  url: canonical,
  provider: {
    '@id': organizationId,
  },
  isPartOf: {
    '@id': websiteId,
  },
  areaServed: {
    '@type': 'Country',
    name: 'Brasil',
  },
  serviceType: 'Gestão de Google Ads e Meta Ads para escritórios de advocacia',
  audience: {
    '@type': 'Audience',
    audienceType: 'Advogados e escritórios de advocacia',
  },
  hasOfferCatalog: {
    '@type': 'OfferCatalog',
    name: 'Áreas jurídicas atendidas',
    itemListElement: legalAreas.map((area) => ({
      '@type': 'Offer',
      itemOffered: {
        '@type': 'Service',
        name: `Tráfego pago para ${area.name}`,
        description: area.metaDescription,
      },
    })),
  },
  brand: {
    '@type': 'Brand',
    name: brand.name,
  },
};

const faqSchema = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  '@id': `${canonical}#faq`,
  isPartOf: {
    '@id': websiteId,
  },
  mainEntity: faqs.map((item) => ({
    '@type': 'Question',
    name: item.question,
    acceptedAnswer: { '@type': 'Answer', text: item.answer },
  })),
};
---

<BaseLayout
  title="Tráfego Pago para Advogados | Motor de Captação Jurídica | Scale"
  description={pageDescription}
  canonical={canonical}
>
  <link
    slot="head"
    rel="preload"
    as="image"
    href="/scale-advogados/assets/desktop.webp"
    type="image/webp"
    media="(min-width: 769px)"
    fetchpriority="high"
  />
  <link
    slot="head"
    rel="preload"
    as="image"
    href="/scale-advogados/assets/mobile%20(2).png"
    type="image/png"
    media="(max-width: 768px)"
    fetchpriority="high"
  />
  <script slot="head" type="application/ld+json" set:html={JSON.stringify(schema)} />
  <script slot="head" type="application/ld+json" set:html={JSON.stringify(faqSchema)} />
  <script slot="head" is:inline>
    (() => {
      const root = document.documentElement;
      root.classList.add('trafego-advogados-page');

      const updateHeaderVisibility = () => {
        root.classList.toggle('trafego-advogados-scrolled', window.scrollY > 80);
      };

      updateHeaderVisibility();
      window.addEventListener('scroll', updateHeaderVisibility, { passive: true });
      window.addEventListener('load', updateHeaderVisibility, { once: true });
    })();
  </script>
  <style slot="head" is:global>
    html.trafego-advogados-page #header-outer {
      transition:
        opacity 260ms ease,
        transform 260ms ease,
        padding 0.4s cubic-bezier(0.16, 1, 0.3, 1),
        top 0.4s cubic-bezier(0.16, 1, 0.3, 1);
    }

    html.trafego-advogados-page:not(.trafego-advogados-scrolled) #header-outer {
      opacity: 0;
      pointer-events: none;
      transform: translateY(-120%);
    }

    html.trafego-advogados-page.trafego-advogados-scrolled #header-outer {
      opacity: 1;
      pointer-events: auto;
      transform: translateY(0);
    }

    html.trafego-advogados-page #main-content {
      padding-top: 0 !important;
    }
  </style>

  <div class="paid-advogados">
    <section class="scale-hero" aria-labelledby="hero-title">
      <picture class="scale-hero__picture" aria-hidden="true">
        <source media="(max-width: 768px)" srcset="/scale-advogados/assets/mobile%20(2).png" />
        <img
          src="/scale-advogados/assets/desktop.webp"
          alt=""
          class="scale-hero__image"
          loading="eager"
          decoding="async"
          fetchpriority="high"
        />
      </picture>
      <div class="scale-container scale-hero__container">
        <div class="scale-hero__content">
          <span class="scale-eyebrow">
            <Landmark size={16} strokeWidth={1.8} />
            O fim da dependência de indicações.
          </span>
          <h1 id="hero-title" class="scale-hero__headline">
            Tráfego pago de alta performance para a advocacia.
          </h1>
          <p class="scale-hero__subheadline">
            A Scale constrói o seu motor de captação: campanhas de fundo de funil, qualificação inteligente e landing pages ultrarrápidas para transformar buscas jurídicas em contratos assinados. Rigor ético, tecnologia premium e previsibilidade de caixa.
          </p>

          <div class="scale-hero__ctas">
            <Button href="/contato" variant="ring" size="lg">
              Solicitar diagnóstico do motor de captação
              <ArrowRight size={17} strokeWidth={2.4} />
            </Button>
          </div>

          <div class="scale-authority" aria-label="Pilares do projeto">
            {heroMetrics.map((metric) => {
              const Icon = metric.Icon;
              return (
                <div class="scale-authority__item">
                  <Icon size={18} strokeWidth={1.8} />
                  <span>{metric.value} · {metric.label}</span>
                </div>
              );
            })}
          </div>
        </div>
      </div>

    </section>

    <LegalLogosMarquee label="Escritórios de advocacia que já escalaram suas operações com o método da Scale" />

    <section class="scale-section scale-section--dark">
      <div class="scale-container">
        <div class="scale-split">
          <div class="scale-section-heading scale-section-heading--left">
            <h2>O seu escritório não é um e-commerce. Por que sua agência trata seus leads como se fossem?</h2>
            <p>
              Agências generalistas compram volume. No jurídico, isso vira WhatsApp cheio de curiosos, demandas sem honorários e triagem desperdiçada.
            </p>
            <p>
              A Scale filtra intenção, negativa termos ruins e orienta cada clique por potencial de contrato, não por vaidade de métrica.
            </p>
            <div style="margin-top: 2rem;">
              <Button href="/contato" variant="ring" size="lg">
                Quero captação qualificada
                <ArrowRight size={17} strokeWidth={2.4} />
              </Button>
            </div>
          </div>

          <div class="scale-card-grid scale-card-grid--two">
            {conversionCards.map((item) => {
              const Icon = item.Icon;
              return (
                <article class="scale-card">
                  <div class="scale-card__icon">
                    <Icon size={24} strokeWidth={1.8} />
                  </div>
                  <h3>{item.title}</h3>
                  <p>{item.desc}</p>
                </article>
              );
            })}
          </div>
        </div>
      </div>
    </section>

    <section class="scale-section scale-section--dark scale-section--conversion">
      <div class="scale-container">
        <div class="scale-conversion-layout">
          <div class="scale-section-heading scale-section-heading--left scale-conversion-copy">
            <span class="scale-kicker">Engenharia da conversão</span>
            <h2>Velocidade e sofisticação: onde a concorrência perde o seu cliente.</h2>
            <p>
              O lead jurídico pesquisa pelo celular no momento da dor: um familiar em desespero, um empresário com risco fiscal, um cliente tentando proteger patrimônio. Se a página for lenta ou confusa, ele volta ao Google. Você paga pelo clique; outro escritório atende o cliente.
            </p>
            <div style="margin-top: 2rem;">
              <Button href="/contato" variant="ring" size="lg">
                Melhorar minha conversão
                <ArrowRight size={17} strokeWidth={2.4} />
              </Button>
            </div>
          </div>

          <div class="scale-conversion-system">
            <div class="scale-conversion-flow" aria-label="Pontos da jornada de conversão">
              {['Busca', 'Página', 'WhatsApp', 'Contrato'].map((step, index) => (
                <div class="scale-conversion-flow__item">
                  <span>{String(index + 1).padStart(2, '0')}</span>
                  <strong>{step}</strong>
                </div>
              ))}
            </div>

            <div class="scale-conversion-list">
              {technologyDifferentials.map((item, index) => {
                const Icon = item.Icon;
                return (
                  <article class="scale-conversion-item">
                    <span class="scale-conversion-item__bg-num" aria-hidden="true">
                      {String(index + 1).padStart(2, '0')}
                    </span>
                    <div class="scale-conversion-item__head">
                      <div class="scale-conversion-icon">
                        <Icon size={22} strokeWidth={1.8} />
                      </div>
                      <h3>{item.title}</h3>
                    </div>
                    <p>{item.desc}</p>
                  </article>
                );
              })}
            </div>
          </div>
        </div>
      </div>
    </section>

    <section id="areas" class="scale-section scale-section--light">
      <div class="scale-container">
        <div class="scale-section-heading">
          <h2>A triagem começa no anúncio. Captação cirúrgica por especialidade.</h2>
          <p>
            Cada área do Direito exige uma isca, uma página e um critério de qualificação diferente. Colocar todas as demandas no mesmo balaio é transformar orçamento em ruído.
          </p>
        </div>

        <div class="scale-area-grid">
          {specialtyHighlights.map((area) => (
            <article class="scale-area-card">
              <div class="scale-area-card__top">
                <div>
                  <h3>{area.title}</h3>
                </div>
                <a href={area.href} class="scale-icon-link" aria-label={`Ver página de ${area.title}`}>
                  <ArrowRight size={16} strokeWidth={2.2} />
                </a>
              </div>
              <p>{area.desc}</p>
              <div class="scale-tags">
                {area.metrics.map((metric) => (
                  <span>{metric}</span>
                ))}
              </div>
              {area.secondaryHref && (
                <a href={area.secondaryHref} class="scale-related-link">
                  Ver cluster relacionado
                  <ArrowRight size={15} strokeWidth={2.2} />
                </a>
              )}
            </article>
          ))}
        </div>
        <div style="margin-top: 4rem; display: flex; justify-content: center;">
          <Button href="/contato" variant="primary" size="lg">
            Atrair clientes qualificados
            <ArrowRight size={17} strokeWidth={2.4} />
          </Button>
        </div>
      </div>
    </section>

    <section class="scale-section scale-section--dark">
      <div class="scale-container">
        <div class="scale-oab-strip">
          <div>
            <div class="scale-oab-strip__head">
              <div class="scale-oab-strip__icon">
                <ShieldCheck size={30} strokeWidth={1.6} />
              </div>
              <h2>O Provimento 205/2021 não é um obstáculo. É o nosso manual de alta conversão.</h2>
            </div>
            <p>
              Crescimento agressivo não precisa flertar com infração ética. A Scale constrói campanhas com autoridade técnica, linguagem sóbria e validação do escritório antes de qualquer anúncio ir para o ar.
            </p>
            <div class="scale-tags scale-tags--dark">
              {[
                'Linguagem informativa',
                'Sem promessa de resultado',
                'Sem sensacionalismo',
                'Validação do escritório',
                ...oabCompliance.principles.slice(1, 3),
              ].map((item) => (
                <span>
                  <BadgeCheck size={13} strokeWidth={2.2} />
                  {item}
                </span>
              ))}
            </div>
          </div>
          <div class="scale-oab-strip__action">
            <Button href="/contato" variant="ring" size="lg">
              Solicitar diagnóstico
              <ArrowRight size={17} strokeWidth={2.4} />
            </Button>
          </div>
        </div>
      </div>
    </section>

    <section class="scale-section scale-section--dark scale-section--team">
      <div class="scale-container">
        <div class="scale-section-heading">
          <span class="scale-kicker">Equipe</span>
          <h2>Mais de 32 profissionais acompanhando o mesmo funil: estratégia, mídia, criação, tecnologia e atendimento.</h2>
          <p>
            Página bonita não sustenta captação. O que sustenta é uma operação olhando mensagem, verba, criativo, velocidade, rastreamento e qualidade do contato toda semana.
          </p>
        </div>
      </div>

      <div class="scale-team-carousel" aria-label="Equipe Scale">
        <div class="scale-team-carousel__track">
          {[...legalTeam, ...legalTeam].map((member, index) => (
            <article class="scale-team-card" aria-hidden={index >= legalTeam.length ? 'true' : 'false'}>
              <div class="scale-team-card__photo">
                <img src={member.image} alt={index >= legalTeam.length ? '' : member.name} loading="lazy" decoding="async" />
              </div>
              <div class="scale-team-card__copy">
                <h3>{member.name}</h3>
                <p>{member.role}</p>
              </div>
            </article>
          ))}
          <article class="scale-team-card scale-team-card--plus" aria-hidden="true">
            <strong>+30</strong>
            <span>Profissionais</span>
          </article>
        </div>
      </div>
    </section>

    <section class="scale-section scale-section--light">
      <div class="scale-container scale-container--narrow">
        <div class="scale-section-heading">
          <span class="scale-kicker">FAQ</span>
          <h2>Dúvidas reais de sócios e gestores.</h2>
        </div>

        <div class="scale-faq">
          {faqs.map((item) => (
            <details class="scale-faq__item">
              <summary>
                {item.question}
                <span>+</span>
              </summary>
              <p>{item.answer}</p>
            </details>
          ))}
        </div>
      </div>
    </section>

    <section class="scale-section scale-section--dark scale-final">
      <div class="scale-container scale-container--narrow">
        <span class="scale-kicker">Diagnóstico</span>
        <h2>O próximo grande contrato do seu escritório já está no Google procurando por uma banca como a sua.</h2>
        <p>
          Pare de perder mercado para bancas com menor qualificação técnica do que a sua, mas com uma engenharia digital superior.
        </p>
        <div class="scale-final__cta">
          <Button href="/contato" variant="ring" size="lg">
            Solicitar o diagnóstico da minha operação
            <ArrowRight size={17} strokeWidth={2.4} />
          </Button>
        </div>
      </div>
    </section>
  </div>

  <style>
    .paid-advogados {
      --scale-bg: #010000;
      --scale-bg-card: #06070a;
      --scale-surface: #0a0c12;
      --scale-border: rgba(255, 255, 255, 0.08);
      --scale-border-strong: rgba(0, 119, 255, 0.32);
      --scale-text: #ffffff;
      --scale-muted: #cbd5e1;
      --scale-soft: rgba(255, 255, 255, 0.66);
      --scale-blue: #0077ff;
      --scale-blue-soft: rgba(0, 119, 255, 0.1);
      --home-bg: #010000;
      background: var(--scale-bg);
      color: var(--scale-text);
      font-family: Inter, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      overflow: hidden;
    }

    .scale-container {
      width: 100%;
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 1.5rem;
      position: relative;
      z-index: 1;
    }

    .scale-container--narrow {
      max-width: 820px;
      text-align: center;
    }

    .scale-hero {
      position: relative;
      display: flex;
      min-height: 92svh;
      margin-top: 0;
      padding: 10rem 0 6.5rem;
      align-items: center;
      overflow: hidden;
      isolation: isolate;
    }

    .scale-hero__picture,
    .scale-hero__image {
      position: absolute;
      inset: 0;
      width: 100%;
      height: 100%;
    }

    .scale-hero__picture {
      z-index: 0;
    }

    .scale-hero__image {
      object-fit: cover;
      object-position: center top;
    }

    .scale-hero__container {
      z-index: 1;
    }

    .scale-hero__content {
      max-width: 620px;
    }

    .scale-eyebrow,
    .scale-kicker,
    .scale-card-label {
      display: inline-flex;
      align-items: center;
      gap: 0.625rem;
      color: var(--scale-blue);
      font-size: 0.75rem;
      font-weight: 800;
      letter-spacing: 0.12em;
      text-transform: uppercase;
    }

    .scale-hero__headline {
      margin: 1rem 0 1.5rem;
      font-size: 2.1rem;
      font-weight: 700;
      line-height: 1.08;
      letter-spacing: 0;
      color: transparent;
      background: linear-gradient(180deg, #ffffff 0%, #b8beca 100%);
      -webkit-background-clip: text;
      background-clip: text;
    }

    .scale-hero__subheadline {
      max-width: 580px;
      margin: 0 0 2rem;
      color: rgba(255, 255, 255, 0.82);
      font-size: 1.05rem;
      line-height: 1.7;
    }

    .scale-hero__ctas,
    .scale-final__cta {
      display: flex;
      flex-wrap: wrap;
      align-items: center;
      gap: 0.9rem;
    }

    .scale-secondary-button {
      display: inline-flex;
      min-height: 52px;
      align-items: center;
      justify-content: center;
      gap: 0.6rem;
      border: 1px solid rgba(255, 255, 255, 0.18);
      padding: 0.95rem 1.4rem;
      color: rgba(255, 255, 255, 0.78);
      font-size: 0.95rem;
      font-weight: 700;
      text-decoration: none;
      transition: border-color 180ms ease, background 180ms ease, color 180ms ease;
    }

    .scale-secondary-button:hover {
      border-color: rgba(255, 255, 255, 0.38);
      background: rgba(255, 255, 255, 0.05);
      color: #ffffff;
    }

    .scale-authority {
      display: flex;
      flex-wrap: wrap;
      gap: 1rem;
      margin-top: 2.2rem;
      color: var(--scale-muted);
      font-size: 0.88rem;
      font-weight: 700;
    }

    .scale-authority__item {
      display: inline-flex;
      align-items: center;
      gap: 0.55rem;
    }

    .scale-authority__item svg {
      color: var(--scale-blue);
      flex: 0 0 auto;
    }

    .scale-section {
      position: relative;
      padding: 6.5rem 0;
      overflow: clip;
      border-top: 1px solid rgba(255, 255, 255, 0.04);
    }

    .scale-section::before {
      content: '';
      position: absolute;
      top: -1px;
      left: 50%;
      width: min(600px, 58vw);
      height: 1px;
      transform: translateX(-50%);
      background: linear-gradient(90deg, transparent, rgba(0, 119, 255, 0.32), transparent);
      pointer-events: none;
    }

    .scale-section--dark {
      background:
        linear-gradient(180deg, rgba(255, 255, 255, 0.018), rgba(255, 255, 255, 0)),
        #010000;
    }

    .scale-section--light {
      background: linear-gradient(180deg, #f5f8fc 0%, #edf3fa 100%);
      color: #0c1424;
      border-top-color: rgba(0, 119, 255, 0.12);
    }

    .scale-section--light::before {
      background: linear-gradient(90deg, transparent, rgba(0, 119, 255, 0.22), transparent);
    }

    .scale-split {
      display: grid;
      grid-template-columns: 0.9fr 1.1fr;
      gap: 4rem;
      align-items: start;
    }

    .scale-split--visual {
      grid-template-columns: 1fr 1fr;
      align-items: center;
    }

    .scale-section-heading {
      max-width: 820px;
      margin: 0 auto 3.5rem;
      text-align: center;
    }

    .scale-section-heading--left {
      margin: 0;
      text-align: left;
    }

    .scale-section-heading h2,
    .scale-final h2 {
      margin: 1rem 0 1.2rem;
      font-size: 2.1rem;
      line-height: 1.13;
      font-weight: 700;
      letter-spacing: 0;
      color: transparent;
      background: linear-gradient(180deg, #ffffff 0%, #b5bbc7 100%);
      -webkit-background-clip: text;
      background-clip: text;
    }

    .scale-section--light .scale-section-heading h2,
    .scale-section--light .scale-area-card h3,
    .scale-section--light .scale-faq summary {
      color: #0c1424;
      background: none;
      -webkit-background-clip: initial;
      background-clip: initial;
    }

    .scale-section-heading p,
    .scale-final p {
      color: var(--scale-soft);
      font-size: 1.02rem;
      line-height: 1.75;
    }

    .scale-section-heading p + p {
      margin-top: 1rem;
    }

    .scale-section--light .scale-section-heading p,
    .scale-section--light .scale-area-card p,
    .scale-section--light .scale-faq p {
      color: #425066;
    }

    .scale-card-grid {
      display: grid;
      gap: 1rem;
    }

    .scale-card-grid--two {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .scale-card,
    .scale-area-card,
    .scale-oab-strip,
    .scale-team-card,
    .scale-faq__item {
      border: 1px solid var(--scale-border);
    }

    .scale-card {
      min-height: 230px;
      padding: 1.6rem;
      background: linear-gradient(145deg, rgba(255, 255, 255, 0.045), rgba(255, 255, 255, 0.012));
      transition: transform 180ms ease, border-color 180ms ease, background 180ms ease;
    }

    .scale-card:hover {
      transform: translateY(-4px);
      border-color: var(--scale-border-strong);
      background: rgba(0, 119, 255, 0.04);
    }

    .scale-card__icon,
    .scale-oab-strip__icon {
      display: flex;
      width: 2.7rem;
      height: 2.7rem;
      align-items: center;
      justify-content: center;
      margin-bottom: 1.15rem;
      background: var(--scale-blue-soft);
      color: var(--scale-blue);
    }

    .scale-card h3,
    .scale-area-card h3,
    .scale-team-card h3 {
      margin: 0 0 0.75rem;
      color: #ffffff;
      font-size: 1.18rem;
      line-height: 1.25;
      font-weight: 800;
      letter-spacing: 0;
    }

    .scale-card p {
      margin: 0;
      color: var(--scale-muted);
      font-size: 0.94rem;
      line-height: 1.65;
    }

    .scale-area-card:hover {
      transform: translateY(-5px);
      border-color: rgba(0, 119, 255, 0.28);
      box-shadow: 0 24px 60px rgba(12, 20, 36, 0.13);
    }

    .scale-card-label {
      margin-bottom: 0.9rem;
      color: var(--scale-blue);
      font-size: 0.68rem;
    }

    .scale-section--conversion {
      background-image:
        linear-gradient(90deg, rgba(0, 119, 255, 0.08) 1px, transparent 1px),
        linear-gradient(180deg, #010000 0%, #05070c 100%);
      background-size: 92px 100%, auto;
    }

    .scale-conversion-layout {
      display: grid;
      grid-template-columns: minmax(0, 0.86fr) minmax(0, 1.14fr);
      gap: clamp(2.4rem, 6vw, 5.5rem);
      align-items: start;
    }

    .scale-conversion-copy {
      position: sticky;
      top: 8rem;
      max-width: 560px;
    }

    .scale-conversion-system {
      display: grid;
      gap: 2.1rem;
    }

    .scale-conversion-flow {
      display: grid;
      grid-template-columns: repeat(4, minmax(0, 1fr));
      background: rgba(255, 255, 255, 0.02);
      border: 1px solid rgba(255, 255, 255, 0.08);
      border-radius: 12px;
      overflow: hidden;
    }

    .scale-conversion-flow__item {
      position: relative;
      display: flex;
      flex-direction: column;
      gap: 0.25rem;
      min-height: 5.6rem;
      align-items: center;
      justify-content: center;
      padding: 1rem 0.5rem;
      text-align: center;
    }

    .scale-conversion-flow__item:not(:last-child) {
      border-right: 1px solid rgba(255, 255, 255, 0.06);
    }

    .scale-conversion-flow__item span {
      color: var(--scale-blue);
      font-size: 0.72rem;
      font-weight: 900;
      letter-spacing: 0.12em;
    }

    .scale-conversion-flow__item strong {
      color: rgba(255, 255, 255, 0.88);
      font-size: 0.9rem;
      font-weight: 800;
      letter-spacing: 0;
    }

    .scale-conversion-list {
      display: flex;
      flex-direction: column;
      gap: 1.5rem;
    }

    .scale-conversion-item {
      position: relative;
      display: block;
      padding: 2.2rem;
      background: rgba(255, 255, 255, 0.02);
      border: 1px solid rgba(255, 255, 255, 0.08);
      border-radius: 12px;
      overflow: hidden;
      backdrop-filter: blur(8px);
      transition: transform 0.3s ease, background 0.3s ease, border-color 0.3s ease;
    }

    .scale-conversion-item:hover {
      background: rgba(255, 255, 255, 0.04);
      border-color: rgba(0, 119, 255, 0.25);
      transform: translateY(-4px);
    }

    .scale-conversion-item__bg-num {
      position: absolute;
      bottom: -15px;
      right: 10px;
      font-size: 8rem;
      font-weight: 800;
      line-height: 1;
      color: rgba(0, 119, 255, 0.05);
      pointer-events: none;
      user-select: none;
    }

    .scale-conversion-item__head {
      display: flex;
      align-items: center;
      gap: 1rem;
      margin-bottom: 1rem;
      position: relative;
      z-index: 1;
    }

    .scale-conversion-icon {
      display: flex;
      width: 3rem;
      height: 3rem;
      flex: 0 0 auto;
      align-items: center;
      justify-content: center;
      background: rgba(0, 119, 255, 0.08);
      border: 1px solid rgba(0, 119, 255, 0.2);
      border-radius: 10px;
      color: var(--scale-blue);
    }

    .scale-conversion-item h3 {
      margin: 0;
      color: #ffffff;
      font-size: 1.25rem;
      line-height: 1.25;
      font-weight: 800;
      letter-spacing: 0;
    }

    .scale-conversion-item p {
      margin: 0;
      color: rgba(255, 255, 255, 0.7);
      font-size: 1rem;
      line-height: 1.6;
      position: relative;
      z-index: 1;
    }

    .scale-area-grid {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 1rem;
    }

    .scale-area-card {
      padding: 1.75rem;
      background: rgba(255, 255, 255, 0.9);
      box-shadow: 0 18px 44px rgba(12, 20, 36, 0.08);
      transition: transform 180ms ease, border-color 180ms ease, box-shadow 180ms ease;
    }

    .scale-area-card__top {
      display: flex;
      align-items: flex-start;
      justify-content: space-between;
      gap: 1.2rem;
      margin-bottom: 1.15rem;
    }

    .scale-area-card h3 {
      margin-top: 0.35rem;
      font-size: 1.45rem;
    }

    .scale-icon-link {
      display: flex;
      width: 2.6rem;
      height: 2.6rem;
      flex: 0 0 auto;
      align-items: center;
      justify-content: center;
      border: 1px solid rgba(12, 20, 36, 0.12);
      color: #0c1424;
      transition: border-color 180ms ease, color 180ms ease, background 180ms ease;
    }

    .scale-icon-link:hover {
      border-color: var(--scale-blue);
      background: rgba(0, 119, 255, 0.08);
      color: var(--scale-blue);
    }

    .scale-tags {
      display: flex;
      flex-wrap: wrap;
      gap: 0.5rem;
      margin-top: 1.25rem;
    }

    .scale-tags span {
      display: inline-flex;
      align-items: center;
      gap: 0.4rem;
      border: 1px solid rgba(12, 20, 36, 0.1);
      background: rgba(12, 20, 36, 0.04);
      padding: 0.42rem 0.7rem;
      color: #4b5a70;
      font-size: 0.75rem;
      font-weight: 750;
    }

    .scale-tags--dark span {
      border-color: rgba(255, 255, 255, 0.1);
      background: rgba(255, 255, 255, 0.04);
      color: rgba(255, 255, 255, 0.74);
    }

    .scale-related-link {
      display: inline-flex;
      align-items: center;
      gap: 0.45rem;
      margin-top: 1.15rem;
      color: var(--scale-blue);
      font-size: 0.9rem;
      font-weight: 800;
      text-decoration: none;
    }

    .scale-oab-strip {
      display: grid;
      grid-template-columns: 1fr auto;
      gap: 2rem;
      align-items: center;
      border-color: rgba(0, 119, 255, 0.25);
      background: rgba(0, 119, 255, 0.03);
      padding: 3rem;
    }

    .scale-oab-strip__head {
      display: flex;
      gap: 1.1rem;
      align-items: center;
      margin-bottom: 1rem;
    }

    .scale-oab-strip__icon {
      width: 3.5rem;
      height: 3.5rem;
      margin: 0;
      flex: 0 0 auto;
    }

    .scale-oab-strip h2 {
      margin: 0;
      color: #ffffff;
      font-size: 2rem;
      line-height: 1.16;
      letter-spacing: 0;
    }

    .scale-oab-strip p {
      max-width: 820px;
      margin: 0;
      color: var(--scale-muted);
      font-size: 1rem;
      line-height: 1.7;
    }

    .scale-oab-strip__action {
      justify-self: end;
    }

    .scale-section--team {
      padding-bottom: 5rem;
    }

    .scale-team-carousel {
      overflow: hidden;
      padding: 1.9rem 0;
      background: linear-gradient(180deg, rgba(0, 119, 255, 0.06), rgba(255, 255, 255, 0.01));
      box-shadow: inset 0 28px 52px rgba(0, 0, 0, 0.46), inset 0 -28px 52px rgba(0, 0, 0, 0.46);
    }

    .scale-team-carousel__track {
      display: flex;
      width: max-content;
      gap: 1rem;
      animation: scaleTeamMove 28s linear infinite;
      will-change: transform;
    }

    .scale-team-carousel:hover .scale-team-carousel__track {
      animation-play-state: paused;
    }

    .scale-team-card {
      width: 260px;
      flex: 0 0 auto;
      overflow: hidden;
      background: linear-gradient(180deg, rgba(255, 255, 255, 0.045), rgba(255, 255, 255, 0.015));
      box-shadow: inset 0 0 24px rgba(0, 0, 0, 0.38), 0 12px 30px rgba(0, 0, 0, 0.22);
    }

    .scale-team-card__photo {
      aspect-ratio: 1 / 1;
      overflow: hidden;
      background: #050505;
    }

    .scale-team-card__photo img {
      width: 100%;
      height: 100%;
      object-fit: contain;
      filter: grayscale(0.12) brightness(0.86);
      transition: filter 180ms ease, transform 180ms ease;
    }

    .scale-team-card:hover img {
      filter: grayscale(0) brightness(1);
      transform: scale(1.03);
    }

    .scale-team-card__copy {
      min-height: 86px;
      padding: 0.9rem 1rem 1rem;
    }

    .scale-team-card h3 {
      margin: 0;
      font-size: 1rem;
    }

    .scale-team-card p {
      margin: 0.45rem 0 0;
      color: var(--scale-blue);
      font-size: 0.62rem;
      font-weight: 850;
      line-height: 1.35;
      letter-spacing: 0.1em;
      text-transform: uppercase;
    }

    .scale-team-card--plus {
      display: flex;
      min-height: 346px;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      border-color: rgba(0, 186, 255, 0.24);
      background: linear-gradient(145deg, rgba(0, 186, 255, 0.13), rgba(255, 255, 255, 0.018));
      text-align: center;
    }

    .scale-team-card--plus strong {
      color: #ffffff;
      font-size: 3.8rem;
      line-height: 1;
      font-weight: 950;
    }

    .scale-team-card--plus span {
      margin-top: 0.6rem;
      color: var(--scale-muted);
      font-size: 0.85rem;
      font-weight: 850;
      letter-spacing: 0.08em;
      text-transform: uppercase;
    }

    .scale-faq {
      border-top: 1px solid rgba(12, 20, 36, 0.1);
    }

    .scale-faq__item {
      border-width: 0 0 1px;
      border-color: rgba(12, 20, 36, 0.1);
      background: transparent;
      text-align: left;
    }

    .scale-faq summary {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 1rem;
      padding: 1.35rem 0;
      cursor: pointer;
      list-style: none;
      font-size: 1.05rem;
      font-weight: 800;
    }

    .scale-faq summary::-webkit-details-marker {
      display: none;
    }

    .scale-faq summary span {
      color: var(--scale-blue);
      font-size: 1.6rem;
      line-height: 1;
      transition: transform 160ms ease;
    }

    .scale-faq__item[open] summary span {
      transform: rotate(45deg);
    }

    .scale-faq p {
      margin: -0.4rem 0 1.35rem;
      font-size: 0.98rem;
      line-height: 1.75;
    }

    .scale-final {
      text-align: center;
      padding-bottom: 7rem;
    }

    .scale-final .scale-kicker {
      justify-content: center;
    }

    .scale-final__cta {
      justify-content: center;
      margin-top: 2rem;
    }

    @keyframes scaleTeamMove {
      from {
        transform: translateX(0);
      }
      to {
        transform: translateX(-50%);
      }
    }

    @media (min-width: 768px) {
      .scale-hero__headline {
        font-size: 3.25rem;
      }

      .scale-section-heading h2,
      .scale-final h2 {
        font-size: 3rem;
      }
    }

    @media (min-width: 1024px) {
      .scale-hero__container {
        max-width: 1600px;
        padding-left: 3rem;
        padding-right: 1.5rem;
      }

      .scale-hero__content {
        max-width: 540px;
      }
    }

    @media (min-width: 1180px) {
      .scale-hero__headline {
        font-size: 3.55rem;
      }

      .scale-section-heading h2,
      .scale-final h2 {
        font-size: 3.15rem;
      }
    }

    @media (max-width: 1024px) {
      .scale-split,
      .scale-split--visual,
      .scale-oab-strip {
        grid-template-columns: 1fr;
      }

      .scale-oab-strip__action {
        justify-self: start;
      }

      .scale-conversion-layout {
        grid-template-columns: 1fr;
      }

      .scale-conversion-copy {
        position: static;
        max-width: 820px;
      }

      .scale-conversion-item {
        grid-template-columns: 3.5rem minmax(0, 1fr);
      }
    }

    @media (max-width: 768px) {
      .scale-container {
        padding: 0 1.25rem;
      }

      .scale-hero {
        min-height: 112svh;
        padding: 8rem 0 8rem;
        align-items: end;
      }

      .scale-hero__image {
        object-fit: contain;
        object-position: center top;
        background: #010000;
      }

      .scale-hero__content {
        max-width: none;
        text-align: center;
      }

      .scale-eyebrow,
      .scale-hero__ctas,
      .scale-authority {
        justify-content: center;
      }

      .scale-hero__headline {
        font-size: 1.85rem;
        line-height: 1.16;
      }

      .scale-hero__subheadline {
        font-size: 0.96rem;
      }

      .scale-secondary-button {
        width: 100%;
        max-width: 280px;
      }

      .scale-section {
        padding: 4.5rem 0;
      }

      .scale-section-heading,
      .scale-section-heading--left {
        text-align: center;
      }

      .scale-section-heading h2,
      .scale-final h2 {
        font-size: 2rem;
      }

      .scale-card-grid--two,
      .scale-area-grid {
        grid-template-columns: 1fr;
      }

      .scale-card {
        min-height: auto;
      }

      .scale-conversion-system {
        gap: 1.6rem;
      }

      .scale-conversion-flow {
        grid-template-columns: repeat(2, minmax(0, 1fr));
      }

      .scale-conversion-flow__item {
        min-height: 4.7rem;
        padding: 0.85rem 1rem;
      }

      .scale-conversion-flow__item:nth-child(2n) {
        border-right: 0;
      }

      .scale-conversion-flow__item:nth-child(-n + 2) {
        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
      }

      .scale-conversion-item {
        grid-template-columns: 1fr;
        gap: 0.65rem;
        padding: 1.25rem 0;
      }

      .scale-conversion-item__marker {
        padding-top: 0;
      }

      .scale-conversion-item__head {
        align-items: flex-start;
      }

      .scale-conversion-item h3 {
        font-size: 1.05rem;
      }

      .scale-oab-strip {
        padding: 1.5rem;
      }

      .scale-oab-strip__head {
        align-items: flex-start;
        flex-direction: column;
      }

      .scale-oab-strip h2 {
        font-size: 1.65rem;
      }

      .scale-team-card {
        width: 210px;
      }

      .scale-team-card--plus {
        min-height: 296px;
      }
    }

    @media (prefers-reduced-motion: reduce) {
      .scale-team-carousel__track {
        animation: none;
      }

      .scale-card,
      .scale-conversion-item,
      .scale-area-card,
      .scale-team-card__photo img {
        transition: none;
      }
    }
  </style>
</BaseLayout>
