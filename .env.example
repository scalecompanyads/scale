# Webhook público para leads (POST JSON). Copie para .env.local em dev.
# Em produção, configure no painel do host (Vercel/Netlify) — não commite URLs reais aqui.
# Ex.: Make.com, Zapier, n8n.
PUBLIC_LEAD_WEBHOOK_URL=
